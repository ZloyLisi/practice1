зд 1
    объявление константы
    объявление переменной
    увеличение переменной на 1
зд2 
    myAge (мой возрас) объявление константы, которая несет в себе мой возраст
зд3
    x -создание константы, которая будет хранить в себе число(возраст) максимального значения
    arithmeticMeanOfAge (среднее арифметическое значение возраста) создание переменной, в которой будет храниться
    начальное значение и после вычитания среднего арифметического значение будет перезаписывать значение переменной
    стр19 момент, где происходит вычитание арифметического значения
    и перезаписывание значения переменной
зд4
    стр22 объявление константы для деления
    стр23 процесс деления ранее созданной константы

при смене значения в "testNuber", значение "evenOdd" изменило свое значение с 0 на 1 и на оборот
    в зависимости от числа, которое поступает в "testNumber
зд5
    стр28 создается переменная в которой мы сначала к 0 добавляет 1
    и чтобы правильно все сработала ставим скобки, после чего, мы должны к сумме добавить 10,
    дабы при умножение на 10 не было проблем мы берем снова в скобки, после чего мы умножаем всю сумму на 10, и
    при смещение вправо мы получим число 13-это и есть результат
зд6
    стр 33 "var" используется, потому что эта переменная далее будет изменяться
зд7
    на место а и б были подставлены числа
зд8
    скобки выставлены по приоритету
зд9
    расчёты были сделаны по формуле (a+b)/n, где a-первая переменная, b-вторая переменная, n-количесвто переменных
зд10
    если при переводе из c в f нужно умножать на 1.8 и добавлять 32, то для перевода из f в c нужно сделать обратные действия